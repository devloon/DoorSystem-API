info:
  name: door-delete
  type: http
  seq: 8

http:
  method: DELETE
  url: http://localhost:8000/api/door/delete?name=test
  params:
    - name: name
      value: test
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
