info:
  name: door-all
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/api/door/all
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
