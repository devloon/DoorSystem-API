info:
  name: door-add
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8000/api/door/add?name=test&location=doesn't really exist&description=for testing purposes
  params:
    - name: name
      value: test
      type: query
    - name: location
      value: doesn't really exist
      type: query
    - name: description
      value: for testing purposes
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
