info:
  name: user-delete
  type: http
  seq: 7

http:
  method: DELETE
  url: http://localhost:8000/api/user/delete?email=john@doe.com
  params:
    - name: email
      value: john@doe.com
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
